# BOM 自動正規化系統 - Python 套件需求
# 安裝方式：pip install -r requirements.txt

# 資料處理
pandas>=1.5.0
openpyxl>=3.0.0
xlrd>=2.0.0

# NER 模型（可選，若不需要 AI 功能可不安裝）
torch>=2.0.0
transformers>=4.30.0

# YAML 規則檔案
pyyaml>=6.0
